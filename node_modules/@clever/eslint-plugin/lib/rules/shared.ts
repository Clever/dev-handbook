import { ESLintUtils } from "@typescript-eslint/experimental-utils";

export const createRule = ESLintUtils.RuleCreator(
  (name) => `https://github.com/Clever/eslint-plugin/blob/master/docs/rules/${name}.md`,
);
